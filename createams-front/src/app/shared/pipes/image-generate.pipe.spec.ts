import { ImageGeneratePipe } from './image-generate.pipe';

describe('ImageGeneratePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageGeneratePipe();
    expect(pipe).toBeTruthy();
  });
});
